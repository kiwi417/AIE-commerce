# AIE-commerce 项目说明

> RuoYi 3.9.2 二次开发项目：**商城（mall）+ AI 客服（DeepSeek + RAG 检索）**

## 1. 项目简介

本项目在若依（RuoYi）前后端分离框架基础上扩展了两个模块：

- **商城（mall）**：门户前台（浏览商品、购物车）+ 后台管理（商品、分类），数据存 MySQL `ry` 库
- **AI 客服（ai-service）**：独立的 Python FastAPI 服务，接入 DeepSeek 大模型，基于商品库做 RAG（检索增强生成）导购问答，前端门户内嵌聊天组件，对话日志落库可回溯

## 2. 技术栈

| 层 | 技术 |
|---|---|
| 后端 | Spring Boot 4.1.0（RuoYi 3.9.2）+ MyBatis + MySQL 8.4 + Redis 6（Docker） |
| 前端 | Vue3 + Vite（若依前端工程），开发端口 80 |
| AI 服务 | Python FastAPI + LangChain-DeepSeek + BGE 中文向量（`bge-small-zh-v1.5`）+ FAISS 向量检索 |
| 评估 | Python 自动化评估脚本（`run_eval.py`），规则判分 |

## 3. 系统架构

### 服务与端口

| 服务 | 端口 | 说明 |
|---|---|---|
| 前端 ruoyi-ui | 80 | `npm run dev`，门户 + 后台管理 |
| 后端 ruoyi-admin | 8080 | Spring Boot，含商城与 AI 代理接口 |
| AI 服务 ai-service | 8082 | FastAPI，对话 + 知识库检索 |
| MySQL | 3306 | Windows 服务名 `MySQL84`，库名 `ry` |
| Redis | 6379 | Docker 容器 `redis`（`redis:6-alpine`） |

### 调用链

```
浏览器(:80)
   │  /ai/chat/**（对话、工具数据、清会话、图片）
   ▼
Java 后端(:8080) ──代理──▶ Python AI 服务(:8082)
   │                            │  DeepSeek API（deepseek-chat）
   │ 商品数据（知识库同步）◀────┘  BGE 向量化 → FAISS 检索
   │                            │  会话历史 → Redis（TTL 1800s）
   ▼
MySQL: ai_chat_log 对话日志落库
```

- 前端不直连 8082，统一走 Java 代理（`application.yml` 的 `ai.base-url` 配置，默认 `http://127.0.0.1:8082`）
- AI 服务的商品知识库定时从 Java 商品接口同步，Java 未启动时保留旧知识库、不崩溃

## 4. 目录结构

```
├── ruoyi-admin/          后端入口（含商城 controller、AI 代理 AiChatController）
├── ruoyi-common/         通用工具
├── ruoyi-framework/      框架核心
├── ruoyi-generator/      代码生成
├── ruoyi-quartz/         定时任务
├── ruoyi-system/         系统管理
├── ruoyi-ui/             前端（src/views/portal 门户、AiChatBox 聊天组件、ai/eval 日志页）
├── ai-service/           Python AI 客服服务
│   ├── app/               FastAPI 应用（config/agents/api/embedder/vector_store/...）
│   ├── .env.example       AI 服务配置模板（复制为 .env 后填写）
│   ├── data/kb/           向量知识库缓存（gitignore）
│   └── run_eval.py        自动化评估脚本
├── sql/                  数据库脚本（见 §6）
├── bin/                  Maven 清洁/打包/运行脚本
├── start-dev.bat         一键启动（前端/后端/Redis/MySQL/AI 可选）
├── stop-dev.bat          一键停止、释放端口
└── 项目说明.md            本文档
```

## 5. 环境要求

| 依赖 | 版本/说明 |
|---|---|
| JDK | 17+（本机使用 JDK 26） |
| Maven | 3.8+ |
| Node.js | 18+，npm 安装前端依赖 |
| Python | 3.10+，AI 服务使用 `ai-service/.venv` 虚拟环境 |
| MySQL | 8.4，Windows 服务名 `MySQL84`（启动需管理员权限） |
| Redis | Docker 容器 `redis`（`docker start redis`） |
| DeepSeek API Key | 在 platform.deepseek.com 申请，填入 `ai-service/.env` |

## 6. 数据库初始化

在 `ry` 库中按顺序导入 `sql/` 下脚本：

| 脚本 | 内容 |
|---|---|
| `ry_20260417.sql` | 若依基础库（用户/角色/菜单等），**必须最先导入** |
| `quartz.sql` | 定时任务表 |
| `mall_admin.sql` / `mall_phase2.sql` | 商城表（商品、分类等） |
| `ai_chat_log.sql` | AI 客服对话日志表 |

## 7. 快速启动

### 一键启动（推荐）

双击 `start-dev.bat`：

1. 自动清理残留进程（内部调用 `stop-dev.bat`）
2. 检查并启动 MySQL84（无管理员权限会自动弹 UAC 提权）
3. 检查并启动 Redis 容器
4. 启动后端（新窗口，`ruoyi-admin\target\ruoyi-admin.jar`）
5. 询问是否启动 AI 服务（8 秒默认不启动，按 Y 启动）
6. 启动前端（新窗口，浏览器自动打开 http://localhost）

停止：双击 `stop-dev.bat`（杀掉 java/vite/npm 进程并释放端口）。

### 手动启动

```bat
:: 1. MySQL（需管理员）
net start MySQL84
:: 2. Redis
docker start redis
:: 3. 后端（首次先打包：bin\package.bat 或 mvn package -DskipTests）
java -jar ruoyi-admin\target\ruoyi-admin.jar
:: 4. AI 服务
cd ai-service && .venv\Scripts\python.exe -m app.main
:: 5. 前端
cd ruoyi-ui && npm run dev
```

访问地址：门户/管理后台 http://localhost，后端 http://localhost:8080，AI 服务 http://127.0.0.1:8082（`/health` 探活）。

## 8. 配置说明

所有密钥/密码均**不进仓库**（gitignore），只保留模板：

| 配置 | 文件 | 说明 |
|---|---|---|
| 数据库密码 | `local-env.bat`（根目录） | `set "DB_PASSWORD=xxx"`，由 start-dev.bat 加载；`application-druid.yml` 用 `${DB_PASSWORD}` 引用 |
| AI 服务密钥 | `ai-service/.env` | 复制 `.env.example` 填写：`DEEPSEEK_API_KEY`、`DEEPSEEK_BASE_URL`、`MODEL`、`HOST/PORT`（默认 8082）、`SESSION_TTL_SECONDS` |
| Java→AI 代理 | `ruoyi-admin/src/main/resources/application.yml` 的 `ai:` 段 | `base-url`（默认 `http://127.0.0.1:8082`）、连接/读取超时 |
| 向量知识库 | `ai-service/.env` 可选 | `EMBEDDING_MODEL`（默认 `BAAI/bge-small-zh-v1.5`）、`HF_HOME`/`HF_ENDPOINT`（HF 镜像）、`VECTOR_BACKEND`（faiss，不可用自动回退 numpy）、`KB_REFRESH_INTERVAL_SECONDS`（默认 600） |

本地敏感信息备份在 `local-secrets.txt`（gitignore），**切勿提交或外发**。

## 9. AI 客服模块说明

### 工作流程

1. **知识库同步**：启动时后台线程 + 每 10 分钟一次，从 Java 商品接口拉取商品 → BGE 中文向量化 → FAISS 向量库（`data/kb/`）；也可手动 `POST /ai/kb/refresh`
2. **意图分类**：每轮对话先用 LLM 分为三类——
   - `CONSULTATION`：商品咨询/推荐/对比（走 RAG 检索）
   - `ANALYTICS`：销量统计、排行榜等分析类
   - `OPERATION`：下单/加购等操作类（引导用户去页面操作，不代执行）
3. **RAG 检索**：问题向量化 → 向量库 top4（相似度阈值 0.35）→ 命中商品随提示词交给 DeepSeek 生成回答，附商品名/价格/库存卡片；未命中明确告知，**禁止编造商品**
4. **会话**：历史存 Redis（TTL 1800s），前端工具数据接口（`/ai/chat/tool-data`）回传商品卡片
5. **日志**：Java 侧把每轮问答写入 MySQL `ai_chat_log`（问题/回答/意图/耗时），后台管理"AI 评估"页可查看

### 接口

| 接口（Java 代理，前端调用） | 说明 |
|---|---|
| `POST /ai/chat/session` | 创建会话 |
| `POST /ai/chat` | 对话（同步） |
| `POST /ai/chat/stream` | 对话（流式） |
| `GET /ai/chat/tool-data` | 会话工具数据（商品卡片） |
| `GET /ai/chat/clear-session` | 清空会话 |
| `POST /ai/chat/image` | 图片消息（视觉理解） |

Python 侧直连接口：`GET /health`、`POST /ai/kb/refresh`、`POST /ai/chat`、`POST /ai/chat/stream`、`GET /ai/chat/tool-data`。

## 10. AI 评估体系

`ai-service/` 下的自动化评估（需 AI 服务已在 8082 运行）：

```bat
cd ai-service
.venv\Scripts\python.exe run_eval.py
```

- 用例：`eval_set.json`（基于实测基线构建）
- 判分规则：`must_contain` / `must_not_contain`（内容断言）、`must_match` / `must_not_match`（正则）、`must_product` / `must_not_product`（商品卡片断言）、`products_empty`、`ungrounded_policy`
- 结果：`eval_report.json`，分级 **FAIL（事实错误）> WARN（已知短板）> PASS**

## 11. 打包部署

```bat
:: 后端 jar（输出到各模块 target/，入口 ruoyi-admin\target\ruoyi-admin.jar）
bin\package.bat
:: 前端产物（输出到 ruoyi-ui\dist）
ruoyi-ui\bin\build.bat
```

## 12. Git 仓库

- 远程：https://github.com/kiwi417/zjw.git（分支 `master`）
- 约定：所有密钥、本地配置、构建产物（`target/`、`node_modules/`、`.venv/`、`data/`、`*.log`）均被 `.gitignore` 排除，仓库内只保留代码与配置模板
