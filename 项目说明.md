# AIE-commerce 项目说明

> RuoYi 3.9.2 前后端分离框架二次开发项目：**商城（mall）+ AI 客服（DeepSeek + RAG 检索）**

## 1. 项目简介

本项目在若依（RuoYi-Vue）框架基础上扩展了两个模块：

- **商城（mall）**：门户前台（商品浏览/搜索、购物车）+ 后台管理（商品、分类管理），商品/分类数据存 MySQL `ry` 库，购物车存 Redis
- **AI 客服（ai-service）**：独立的 Python FastAPI 服务，接入 DeepSeek 大模型，基于商品库做 RAG（检索增强生成）导购问答；前端门户内嵌聊天组件，对话日志落库可回溯

## 2. 系统架构

### 2.1 服务与端口

| 服务 | 端口 | 技术 | 说明 |
|---|---|---|---|
| 前端 ruoyi-ui | 80 | Vue3 + Vite6 | `npm run dev`，门户（免登录）+ 后台管理 |
| 后端 ruoyi-admin | 8080 | Spring Boot 4.1.0 | 商城接口 + AI 代理接口 |
| AI 服务 ai-service | 8082 | Python FastAPI | 对话 + 意图分类 + 知识库检索 |
| MySQL | 3306 | MySQL 8.4 | Windows 服务名 `MySQL84`，库名 `ry` |
| Redis | 6379 | Redis 6（Docker 容器 `redis`） | 会话历史、检索上下文、购物车 |

### 2.2 调用链

```
浏览器(:80, 门户/AiChatBox 组件)
   │  /ai/chat/**（对话/流式/工具数据/清会话/图片）
   ▼
Java 后端(:8080) ──HttpClient 代理──▶ Python AI 服务(:8082)
   │                                    │  DeepSeek API（deepseek-chat）
   │ 商品数据（知识库定时同步）◀──────────┘  BGE 向量化 → FAISS 检索
   │                                    │  会话历史/检索上下文 → Redis（TTL 1800s）
   ▼
MySQL: mall_product/mall_category（商品）
       ai_chat_log（对话日志）
Redis: mall:cart:{sessionId}（购物车）
```

- 前端不直连 8082，统一走 Java 代理（`application.yml` 的 `ai.base-url`，默认 `http://127.0.0.1:8082`）
- Java 与 Python 共用同一个 Redis：Java 直接删 `ai:session:`/`ai:ctx:` key 即可清会话，无需转发 Python
- AI 服务商品知识库定时从 Java 商品接口同步，Java 未启动时保留旧知识库、不崩溃

## 3. 技术栈详解

### 3.1 后端（Java）

| 技术 | 版本 | 用途 | 在项目中的位置 |
|---|---|---|---|
| Spring Boot | 4.1.0 | 应用框架 | 版本声明见根 [pom.xml](pom.xml)；启动类 `ruoyi-admin/src/main/java/com/ruoyi/RuoYiApplication.java` |
| RuoYi 框架 | 3.9.2 | 基础工程（RBAC 权限、登录、日志、监控） | 多模块 Maven 工程：`ruoyi-common`（工具）、`ruoyi-framework`（核心）、`ruoyi-system`（系统管理）、`ruoyi-quartz`（定时任务）、`ruoyi-generator`（代码生成）、`ruoyi-admin`（入口） |
| Spring Security | — | 登录认证、权限校验；`@Anonymous` 注解放行门户接口 | `ruoyi-framework/src/main/java/com/ruoyi/framework/web/service/`、`.../config/SecurityConfig.java` |
| MyBatis | 4.1.0（mybatis-spring-boot-starter） | ORM 持久层 | 商城 Mapper XML：[AiChatLogMapper.xml](ruoyi-admin/src/main/resources/mapper/mall/AiChatLogMapper.xml)、[MallCategoryMapper.xml](ruoyi-admin/src/main/resources/mapper/mall/MallCategoryMapper.xml)、[MallProductMapper.xml](ruoyi-admin/src/main/resources/mapper/mall/MallProductMapper.xml) |
| Druid | 1.2.28 | 数据库连接池 + SQL 监控 | [application-druid.yml](ruoyi-admin/src/main/resources/application-druid.yml) |
| PageHelper | 4.1.1 | 物理分页 | 全局配置 `ruoyi-framework` 的 MyBatis 配置类；商品分页查询 `PortalProductController#page` |
| fastjson2 | 2.0.64 | JSON 序列化/解析 | AI 代理转发中解析 Python 响应，见 [AiChatServiceImpl.java](ruoyi-admin/src/main/java/com/ruoyi/mall/service/impl/AiChatServiceImpl.java) |
| JJWT | 0.9.1 | JWT 令牌生成与解析 | `ruoyi-framework` 的 `TokenService` |
| Kaptcha | 2.3.3 | 图形验证码 | [CaptchaController.java](ruoyi-admin/src/main/java/com/ruoyi/web/controller/common/CaptchaController.java) |
| Quartz | — | 定时任务管理 | `ruoyi-quartz` 模块 |
| Springdoc OpenAPI | 3.1.0 | API 文档（/swagger-ui.html） | [SwaggerConfig.java](ruoyi-admin/src/main/java/com/ruoyi/web/core/config/SwaggerConfig.java)；前端 vite 有 /v3/api-docs 代理 |
| Apache POI | 5.5.1 | Excel 导入导出 | `ruoyi-common` 的 ExcelUtil |
| Velocity | 2.3 | 代码生成模板引擎 | `ruoyi-generator` 模块 |
| OSHI | 7.4.4 | 服务器硬件监控 | [ServerController.java](ruoyi-admin/src/main/java/com/ruoyi/web/controller/monitor/ServerController.java) |
| JDK HttpClient（java.net.http） | JDK 17+ 内置 | **AI 代理转发**：Java→Python 8082 的 HTTP 调用与流式字节转发 | [AiChatServiceImpl.java](ruoyi-admin/src/main/java/com/ruoyi/mall/service/impl/AiChatServiceImpl.java)（连接超时/读超时来自 `ai:` 配置段） |
| StreamingResponseBody | Spring 内置 | 流式响应：Python 的裸文本流逐字节原样转发给前端 | 同上 |
| RedisCache | RuoYi 封装 | Redis 读写：购物车、清会话 | `ruoyi-common` 的 `RedisCache`；购物车见 [PortalCartServiceImpl.java](ruoyi-admin/src/main/java/com/ruoyi/mall/service/impl/PortalCartServiceImpl.java) |
| Maven | 3.8+ | 构建（阿里云镜像加速） | [pom.xml](pom.xml) 的 `<repositories>` |

### 3.2 前端（Vue）

| 技术 | 版本 | 用途 | 在项目中的位置 |
|---|---|---|---|
| Vue | 3.5.26 | 视图框架（组合式 API） | [ruoyi-ui/src/](ruoyi-ui/src/) 全部视图 |
| Vite | 6.4.1 | 构建/开发服务器（端口 80，代理 /dev-api → 后端） | [vite.config.js](ruoyi-ui/vite.config.js) |
| Element Plus | 2.13.1 | UI 组件库 | 全局注册见 `ruoyi-ui/src/plugins/index.js` |
| Pinia | 3.0.4 | 状态管理（登录态、权限等） | [ruoyi-ui/src/store/](ruoyi-ui/src/store/) |
| Vue Router | 4.6.4 | 路由；门户页面在此注册为免登录路由 | [index.js](ruoyi-ui/src/router/index.js)（`/portal/**` 路由见 75-116 行） |
| Axios | 1.13.2 | HTTP 请求封装（拦截器带 token） | [ruoyi-ui/src/utils/request.js](ruoyi-ui/src/utils/request.js) |
| 原生 fetch + TextDecoder | 浏览器内置 | **AI 流式渲染**：读取裸文本流按字节解码，逐字符追加（不能用 axios/SSE） | [AiChatBox/index.vue](ruoyi-ui/src/components/AiChatBox/index.vue) |
| ECharts | 5.6.0 | 图表（后台统计页） | `ruoyi-ui/src/views/` 统计类页面 |
| Sass（sass-embedded） | 1.97.2 | 样式预处理 | 各 `.vue` 组件的 scss 样式 |
| jsencrypt | 3.3.2 | 登录密码 RSA 加密 | `ruoyi-ui/src/utils/jsencrypt.js` |
| @vueup/vue-quill | 1.2.0 | 富文本编辑器（商品描述等） | `ruoyi-ui/src/components/Editor/` |
| vue-cropper | 1.1.1 | 头像裁剪 | `ruoyi-ui/src/views/system/user/profile/` |
| vuedraggable | 4.1.0 | 拖拽排序 | 后台表单拖拽组件 |
| unplugin-auto-import / unplugin-vue-setup-extend-plus | 0.18.6 / 1.0.1 | 自动导入 Vue API | `ruoyi-ui/vite/plugins/` |
| vite-plugin-svg-icons | 2.0.1 | SVG 图标 | `ruoyi-ui/src/components/SvgIcon/` |

### 3.3 AI 服务（Python）

依赖清单见 [requirements.txt](ai-service/requirements.txt)，全部文件位于 `ai-service/app/`：

| 技术 | 版本 | 用途 | 在项目中的位置 |
|---|---|---|---|
| FastAPI + Uvicorn | ≥0.115 | Web 框架与 ASGI 服务器 | 端点定义 [api.py](ai-service/app/api.py)；启动入口 [main.py](ai-service/app/main.py) |
| LangChain-DeepSeek | ≥0.1.3 | DeepSeek 大模型接入（`ChatDeepSeek`） | [agents.py](ai-service/app/agents.py)：对话 LLM（temperature 0.7）+ 分类 LLM（temperature 0、max_tokens 32） |
| langchain-core | ≥0.3 | 消息模型（System/Human/AI Message） | 同上，`build_messages()` |
| Pydantic + pydantic-settings | ≥2.10 / ≥2.7 | 配置管理与请求体校验 | [config.py](ai-service/app/config.py)（从 `.env` 读全部配置）；`ChatRequest`/`StreamRequest` 见 api.py |
| sentence-transformers | ≥6.0 | 向量化：BGE 中文模型 `BAAI/bge-small-zh-v1.5`（512 维） | [embedder.py](ai-service/app/embedder.py)（惰性单例，支持 HF 镜像） |
| faiss-cpu | ≥1.15 | 向量检索（IndexFlatIP 内积=余弦） | [vector_store.py](ai-service/app/vector_store.py) |
| numpy | ≥2.0 | faiss 不可用时的暴力检索兜底；向量矩阵存取 | 同上（NumpyVectorStore） |
| redis（Python 客户端） | ≥5.2 | 会话历史 + 检索上下文存储 | [redis_store.py](ai-service/app/redis_store.py)、[session_store.py](ai-service/app/session_store.py) |
| httpx | — | 从 Java 拉取商品/分类数据 | [product_sync.py](ai-service/app/product_sync.py) |
| threading（标准库） | — | 知识库后台同步线程（启动即同步 + 定时刷新） | 同上，`background_loop()` |

### 3.4 基础设施与存储

| 组件 | 版本 | 用途 |
|---|---|---|
| MySQL | 8.4（Windows 服务 `MySQL84`） | 业务库 `ry`：若依基础表 + 商城表 + AI 对话日志表 |
| Redis | 6（Docker 容器 `redis`，镜像 `redis:6-alpine`） | Java 与 Python 共用：`ai:session:{id}`（会话历史）、`ai:ctx:{id}`（检索上下文/商品卡片）、`mall:cart:{id}`（购物车） |
| DeepSeek API | `deepseek-chat` 模型 | 对话生成 + 意图分类 |
| HuggingFace | BGE 模型下载（默认直连，可配 `HF_ENDPOINT=https://hf-mirror.com` 镜像） | 向量模型文件（约 130MB，缓存目录可配 `HF_HOME` 重定向到 D 盘） |

## 4. 目录结构

```
├── pom.xml                 Maven 父工程（依赖版本统一管理，阿里云镜像）
├── ruoyi-admin/            ★ 后端入口：Spring Boot 启动 + 全部业务 Controller
│   └── src/main/
│       ├── java/com/ruoyi/
│       │   ├── mall/       ★ 商城 + AI 客服模块（本次开发核心，见 §5.1）
│       │   └── web/        若依自带：登录/系统/监控/工具 Controller
│       └── resources/
│           ├── application.yml         端口、AI 代理配置（ai: 段）
│           ├── application-druid.yml   数据库连接（密码走 ${DB_PASSWORD}）
│           └── mapper/mall/            商城 Mapper XML
├── ruoyi-common/           通用工具（Redis 缓存、注解、枚举等）
├── ruoyi-framework/        框架核心（Security、拦截器、Token 等）
├── ruoyi-generator/        代码生成器
├── ruoyi-quartz/           定时任务
├── ruoyi-system/           系统管理（用户/角色/菜单/字典等）
├── ruoyi-ui/               ★ 前端工程（Vue3 + Vite）
│   ├── vite.config.js      端口 80 + /dev-api 代理到后端
│   └── src/
│       ├── views/portal/   ★ 门户前台（见 §5.3）
│       ├── views/mall/     后台：商品/分类管理页
│       ├── views/ai/       后台：AI 对话日志、评估、FAQ、知识库、排行榜页
│       ├── components/AiChatBox/  ★ AI 聊天组件（门户内嵌）
│       ├── api/portal/     门户接口定义（商品/购物车/AI 对话）
│       ├── api/mall/       后台商品/分类接口定义
│       ├── api/ai/         AI 相关接口定义
│       └── router/index.js 门户路由注册
├── ai-service/             ★ Python AI 客服服务
│   ├── app/
│   │   ├── main.py         启动入口（uvicorn）
│   │   ├── api.py          FastAPI 端点（对话/流式/工具数据/知识库刷新）
│   │   ├── agents.py       LLM 实例、意图分类、RAG 提示词构建
│   │   ├── config.py       全部配置（pydantic-settings 读 .env）
│   │   ├── embedder.py     BGE 向量化（惰性加载）
│   │   ├── vector_store.py FAISS / numpy 双后端向量库
│   │   ├── product_sync.py 商品知识库同步（后台线程 + 定时 + 手动）
│   │   ├── redis_store.py / session_store.py  会话与检索上下文存储
│   ├── .env.example        AI 服务配置模板（复制为 .env 后填写）
│   ├── requirements.txt    Python 依赖
│   ├── data/kb/            向量知识库落盘缓存（gitignore）
│   ├── eval_set.json       评估用例集
│   └── run_eval.py         ★ 自动化评估脚本（见 §10）
├── sql/                    ★ 数据库脚本（见 §6）
├── bin/                    Maven 清洁/打包/运行脚本
├── start-dev.bat           一键启动（前端/后端/Redis/MySQL/AI 可选）
├── stop-dev.bat            一键停止、释放端口
└── 项目说明.md             本文档
```

## 5. 模块代码地图

### 5.1 商城 + AI 模块（`ruoyi-admin/src/main/java/com/ruoyi/mall/`）

**Controller 层**

| 文件 | 路由前缀 | 说明 |
|---|---|---|
| [AiChatController.java](ruoyi-admin/src/main/java/com/ruoyi/mall/controller/AiChatController.java) | `/ai/chat` | AI 客服门户接口（`@Anonymous` 免登录）：建会话、非流式/流式对话、工具数据、清会话、图片 |
| [AiChatLogController.java](ruoyi-admin/src/main/java/com/ruoyi/mall/controller/AiChatLogController.java) | `/ai/chatlog` | 后台对话日志查询/详情/删除 |
| [MallProductController.java](ruoyi-admin/src/main/java/com/ruoyi/mall/controller/MallProductController.java) | `/mall/product` | 后台商品增删改查 |
| [MallCategoryController.java](ruoyi-admin/src/main/java/com/ruoyi/mall/controller/MallCategoryController.java) | `/mall/category` | 后台分类增删改查 |
| [PortalProductController.java](ruoyi-admin/src/main/java/com/ruoyi/mall/controller/PortalProductController.java) | `/portal/product` | 门户商品分页/搜索/详情 |
| [PortalCategoryController.java](ruoyi-admin/src/main/java/com/ruoyi/mall/controller/PortalCategoryController.java) | `/portal/category` | 门户分类列表 |
| [PortalCartController.java](ruoyi-admin/src/main/java/com/ruoyi/mall/controller/PortalCartController.java) | `/portal/cart` | 门户购物车：加购/列表/改数量/删除/清空（以 sessionId 为购物车标识） |

**Service / Domain / Mapper**

- AI 代理：[AiChatServiceImpl.java](ruoyi-admin/src/main/java/com/ruoyi/mall/service/impl/AiChatServiceImpl.java)
  - `createSession()`：UUID 去横线生成会话 ID
  - `chat()`：非流式对话，转发 Python `/ai/chat`，返回答案 + 商品卡片，并写 `ai_chat_log`
  - `streamChat()`：两阶段流式转发——先建立连接拿到 `X-Intent`/`X-Agent` 响应头，再把响应体**按字节原样转发**（不逐块解码，避免中文多字节被 chunk 边界切断）；流结束无论成败都写日志
  - `toolData()`：转发 Python 工具数据，并**合并 Java Redis 购物车**（`mergeCartItems`），Python 不可用时返回空结构保证前端轮询不报错
  - `clearSession()`：Java 直接删 Redis 的 `ai:session:`/`ai:ctx:` key（与 Python 共用同一 Redis），**不清购物车**
- 购物车：[PortalCartServiceImpl.java](ruoyi-admin/src/main/java/com/ruoyi/mall/service/impl/PortalCartServiceImpl.java) —— Redis key `mall:cart:{sessionId}`，TTL 见 [MallCacheConstants.java](ruoyi-admin/src/main/java/com/ruoyi/mall/constant/MallCacheConstants.java)
- 领域对象：[AiChatLog.java](ruoyi-admin/src/main/java/com/ruoyi/mall/domain/AiChatLog.java)、`AiChatRequest`/`AiChatStreamRequest`（对话请求）、`CartItem`/`CartRequest`、`MallCategory`/`MallProduct`
- Mapper 接口与 XML：`mapper/` 目录 + [resources/mapper/mall/](ruoyi-admin/src/main/resources/mapper/mall/)

### 5.2 前端门户与 AI 组件

| 文件 | 路由 | 说明 |
|---|---|---|
| [portal/index.vue](ruoyi-ui/src/views/portal/index.vue) | `/portal/product`（门户首页） | 商品列表 + 搜索 + 内嵌 AiChatBox 聊天组件 |
| [portal/product/detail.vue](ruoyi-ui/src/views/portal/product/detail.vue) | `/portal/product/detail/:id` | 商品详情（加购入口） |
| [portal/cart/index.vue](ruoyi-ui/src/views/portal/cart/index.vue) | `/portal/cart` | 购物车页 |
| [portal/chat/index.vue](ruoyi-ui/src/views/portal/chat/index.vue) | `/portal/chat` | 独立聊天页（当前路由重定向到门户首页） |
| [portal/order/confirm.vue](ruoyi-ui/src/views/portal/order/confirm.vue) | `/portal/order/confirm` | 订单确认页（前端预留，后端未实现，见 §13） |
| [portal/order/query.vue](ruoyi-ui/src/views/portal/order/query.vue) | `/portal/order/query` | 订单查询页（同上） |
| [AiChatBox/index.vue](ruoyi-ui/src/components/AiChatBox/index.vue) | 组件 | ★ 聊天核心组件：`fetch` 读裸文本流 + `TextDecoder` 流式渲染；发消息后轮询 `/ai/chat/tool-data` 渲染商品卡片；支持图片上传（占位）、清空会话、购物车卡片展示 |
| [api/portal/index.js](ruoyi-ui/src/api/portal/index.js) | — | 门户全部接口定义（分类/商品/AI 对话/购物车/订单） |

门户路由均注册在 [router/index.js](ruoyi-ui/src/router/index.js) 75-116 行，`hidden: true` 且无 Layout 包裹 → **免登录访问**（后端对应接口用 `@Anonymous` 注解放行）。

### 5.3 AI 服务内部（`ai-service/app/`）

| 文件 | 职责 | 关键实现 |
|---|---|---|
| [config.py](ai-service/app/config.py) | 配置中心 | pydantic-settings 读 `.env`：DeepSeek key/模型/超时、Redis、向量库、知识库刷新间隔、检索 top-k 与阈值、Java 地址 |
| [agents.py](ai-service/app/agents.py) | LLM 与意图分类 | 两个惰性单例 LLM（对话/分类）；`classify_intent()` 分类失败一律降级 `CONSULTATION`；`build_rag_context()` 把检索结果拼成【商品库检索结果】提示词块；系统提示词约束"禁止编造商品" |
| [embedder.py](ai-service/app/embedder.py) | 文本向量化 | `BAAI/bge-small-zh-v1.5`，CPU 推理，向量归一化（配合内积=余弦）；模型加载失败只降级知识库不影响聊天 |
| [vector_store.py](ai-service/app/vector_store.py) | 向量库 | FAISS `IndexFlatIP` 优先，numpy 矩阵点积暴力检索兜底，两者语义一致；落盘 `data/kb/`（faiss：`index.faiss`+`ids.npy`；numpy：`matrix.npy`+`ids.npy`） |
| [product_sync.py](ai-service/app/product_sync.py) | 商品知识库 | 后台线程：启动立即同步 + 每 600s 一次；从 Java `/portal/category/list`、`/portal/product/page` 拉数据（3 次重试）；商品拼成"名称/分类/价格/库存/标签/货架/描述"文本后向量化；手动刷新 `POST /ai/kb/refresh` |
| [api.py](ai-service/app/api.py) | HTTP 端点 | `/health`、`/ai/kb/refresh`、`/ai/chat`、`/ai/chat/stream`、`/ai/chat/tool-data`；流式接口返回**裸文本流**（非 SSE），意图经 `X-Intent`/`X-Agent` 响应头传出 |
| [redis_store.py](ai-service/app/redis_store.py) / [session_store.py](ai-service/app/session_store.py) | 会话存储 | 会话历史（`ai:session:`）+ 检索上下文（`ai:ctx:`），TTL 1800s；Redis 不可用时回退内存 |

## 6. 数据库

在 `ry` 库中按顺序导入 `sql/` 下脚本：

| 脚本 | 内容 |
|---|---|
| `ry_20260417.sql` | 若依基础库（sys_user/sys_role/sys_menu/sys_dict 等），**必须最先导入** |
| `quartz.sql` | 定时任务表（QRTZ_*） |
| `mall_admin.sql` / `mall_phase2.sql` | 商城表：`mall_category`（分类）、`mall_product`（商品：名称/价格/库存/标签/货架/描述/图片等） |
| `ai_chat_log.sql` | AI 对话日志表：`ai_chat_log`（session_id、question、answer、intent、agent、latency_ms、创建人/时间） |

## 7. 配置说明

所有密钥/密码均**不进仓库**（gitignore），只保留模板：

| 配置 | 文件 | 说明 |
|---|---|---|
| 数据库密码 | `local-env.bat`（根目录） | `set "DB_PASSWORD=xxx"`，由 start-dev.bat 加载；`application-druid.yml` 用 `${DB_PASSWORD}` 引用 |
| AI 服务密钥 | `ai-service/.env` | 复制 `.env.example` 填写：`DEEPSEEK_API_KEY`、`DEEPSEEK_BASE_URL`、`MODEL`（默认 deepseek-chat）、`HOST/PORT`（默认 8082）、`SESSION_TTL_SECONDS` |
| Java→AI 代理 | [application.yml](ruoyi-admin/src/main/resources/application.yml) 的 `ai:` 段 | `base-url`（默认 `http://127.0.0.1:8082`）、`connect-timeout-ms`（2000）、`read-timeout-ms`（120000） |
| 向量知识库 | `ai-service/.env` 可选 | `EMBEDDING_MODEL`（默认 `BAAI/bge-small-zh-v1.5`）、`HF_HOME`/`HF_ENDPOINT`（HF 镜像）、`VECTOR_BACKEND`（faiss，不可用自动回退 numpy）、`KB_REFRESH_INTERVAL_SECONDS`（默认 600）、`RETRIEVAL_TOP_K`（4）、`RETRIEVAL_SCORE_THRESHOLD`（0.35）、`JAVA_BASE_URL`（商品数据源） |

本地敏感信息备份在 `local-secrets.txt`（gitignore），**切勿提交或外发**。

## 8. 环境要求

| 依赖 | 版本/说明 |
|---|---|
| JDK | 17+（pom 编译目标 17，本机使用 JDK 26） |
| Maven | 3.8+ |
| Node.js | 18+，npm 安装前端依赖 |
| Python | 3.10+，AI 服务使用 `ai-service/.venv` 虚拟环境 |
| MySQL | 8.4，Windows 服务名 `MySQL84`（启动需管理员权限） |
| Redis | Docker 容器 `redis`（`docker start redis`） |
| DeepSeek API Key | 在 platform.deepseek.com 申请，填入 `ai-service/.env` |

## 9. 快速启动

### 一键启动（推荐）

双击 `start-dev.bat`：

1. 自动清理残留进程（内部调用 `stop-dev.bat`）
2. 检查并启动 MySQL84（无管理员权限会自动弹 UAC 提权）
3. 检查并启动 Redis 容器
4. 启动后端（新窗口，`ruoyi-admin\target\ruoyi-admin.jar`）
5. 询问是否启动 AI 服务（8 秒默认不启动，按 Y 启动）
6. 启动前端（新窗口，浏览器自动打开 http://localhost）

停止：双击 `stop-dev.bat`（杀掉 java/vite/npm 进程并释放端口）。

### 手动启动

```bat
:: 1. MySQL（需管理员）
net start MySQL84
:: 2. Redis
docker start redis
:: 3. 后端（首次先打包：bin\package.bat 或 mvn package -DskipTests）
java -jar ruoyi-admin\target\ruoyi-admin.jar
:: 4. AI 服务
cd ai-service && .venv\Scripts\python.exe -m app.main
:: 5. 前端
cd ruoyi-ui && npm run dev
```

访问地址：门户/管理后台 http://localhost，后端 http://localhost:8080，AI 服务 http://127.0.0.1:8082（`/health` 探活）。

## 10. AI 客服模块说明

### 10.1 工作流程（一轮对话）

1. **会话**：前端先 `POST /ai/chat/session` 拿 sessionId（Java 生成 UUID），后续请求都带它
2. **意图分类**：Python 用低温度分类 LLM 把问题分为三类——
   - `CONSULTATION`：商品咨询/推荐/对比（**只有这类走 RAG 检索**）
   - `ANALYTICS`：销量统计、排行榜等分析类
   - `OPERATION`：下单/加购等操作类（引导用户去页面操作，不代执行）
   - 分类失败（超时/解析异常）降级为 `CONSULTATION`，绝不阻塞主链路
3. **RAG 检索**（仅 CONSULTATION）：问题 BGE 向量化 → FAISS top4（相似度阈值 0.35）→ 命中商品拼成【商品库检索结果】提示词块交给 DeepSeek 生成回答，附商品名/价格/库存卡片；未命中明确告知，**禁止编造商品**
4. **流式返回**：Python 返回裸文本流（`text/plain`），意图经 `X-Intent`/`X-Agent` 响应头传出；Java 按字节原样转发；前端 fetch + TextDecoder 逐字符渲染
5. **工具数据**：检索上下文先写入 Redis `ai:ctx:{sessionId}`（流开始前落库），前端流结束后轮询 `/ai/chat/tool-data` 拿商品卡片渲染；Java 在转发时合并 Redis 购物车
6. **日志**：Java 侧把每轮问答写入 MySQL `ai_chat_log`（问题/回答/意图/耗时，流式中断也落库），后台"AI 对话日志"页可查看

### 10.2 接口

| 接口（Java 代理，前端调用） | 说明 |
|---|---|
| `POST /ai/chat/session` | 创建会话 |
| `POST /ai/chat` | 对话（同步） |
| `POST /ai/chat/stream` | 对话（流式，全项目唯一不返回 AjaxResult JSON 的端点） |
| `GET /ai/chat/tool-data` | 会话工具数据（商品卡片 + 购物车） |
| `GET /ai/chat/clear-session` | 清空会话（保留购物车） |
| `POST /ai/chat/image` | 图片消息（占位实现，见 §13） |
| `GET /ai/chatlog/list` 等 | 后台对话日志查询 |

Python 侧直连接口：`GET /health`、`POST /ai/kb/refresh`、`POST /ai/chat`、`POST /ai/chat/stream`、`GET /ai/chat/tool-data`。

### 10.3 容错设计

- 模型加载失败 → 知识库降级不可用，聊天链路不受影响
- LLM 调用失败 → 返回"抱歉"兜底文案（流式若尚未产出内容也兜底）
- Python 服务不可用 → Java 流式接口仍返回 200 + 纯文本兜底文案；tool-data 返回空结构
- 知识库同步失败 → 保留旧索引，绝不崩溃
- 日志落库失败 → 仅告警，不影响对话

## 11. AI 评估体系

`ai-service/` 下的自动化评估（需 AI 服务已在 8082 运行）：

```bat
cd ai-service
.venv\Scripts\python.exe run_eval.py
```

- 用例：`eval_set.json`（基于实测基线构建）
- 判分规则（`run_eval.py`）：
  - `must_contain` / `must_not_contain`：答案内容断言
  - `must_match` / `must_not_match`：正则断言
  - `must_product` / `must_not_product`：商品卡片断言
  - `products_empty`：预期不召回商品（实际召回记 WARN：检索误触发）
  - `ungrounded_policy`：政策类问题无店铺政策文档（记 WARN：答案未锚定风险）
- 结果：`eval_report.json`，分级 **FAIL（事实错误）> WARN（已知短板）> PASS**，存在 FAIL 时退出码 1

## 12. 打包部署

```bat
:: 后端 jar（输出到各模块 target/，入口 ruoyi-admin\target\ruoyi-admin.jar）
bin\package.bat
:: 前端产物（输出到 ruoyi-ui\dist）
ruoyi-ui\bin\build.bat
```

## 13. 当前阶段边界与后续规划

以下为**前端已预留、后端未实现**的部分，便于后续迭代：

| 项 | 现状 | 位置 |
|---|---|---|
| 图片消息 | 占位实现：Java `/ai/chat/image` 返回"功能建设中"文案（DeepSeek 无视觉能力） | [AiChatController.java](ruoyi-admin/src/main/java/com/ruoyi/mall/controller/AiChatController.java) |
| 订单（确认/查询） | 前端页面与 API 定义已有，后端 `/portal/order/*` 未实现 | [portal/order/](ruoyi-ui/src/views/portal/order/)、[api/portal/index.js](ruoyi-ui/src/api/portal/index.js) |
| 知识库文档、FAQ、排行榜管理页 | 前端页面与 API 定义已有，后端控制器未实现 | [views/ai/](ruoyi-ui/src/views/ai/)、[api/ai/](ruoyi-ui/src/api/ai/) |
| AI 评估页 | 前端页面已有（含 RAG/SQL 评分维度），后端 `/ai/eval/*` 未实现 | [views/ai/eval/index.vue](ruoyi-ui/src/views/ai/eval/index.vue) |
| 意图分派 | `ANALYTICS`/`OPERATION` 当前与 `CONSULTATION` 同样只做对话回复；RAG/TextToSQL/Tools 分派是后续阶段 | [agents.py](ai-service/app/agents.py) 注释 |
| business 系列页面 | 早期业务模块前端残留（product/order/stock 等），无对应后端，与 mall 模块并存但未启用 | [views/business/](ruoyi-ui/src/views/business/)、[api/business/](ruoyi-ui/src/api/business/) |

## 14. Git 仓库

- 远程：https://github.com/kiwi417/zjw.git（分支 `master`）
- 约定：所有密钥、本地配置、构建产物（`target/`、`node_modules/`、`.venv/`、`data/`、`*.log`）均被 `.gitignore` 排除，仓库内只保留代码与配置模板
